module(load="imtcp")
input(type="imtcp" port="{{rsyslog_remote_port}}")

if ($fromhost-ip != '127.0.0.1') then {
    action(
        type="omfile"
        file="{{rsyslog_receive_path}}/{{serverb_log_name}}"
    )
    & stop
}